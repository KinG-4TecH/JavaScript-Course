var languages = ["JavaScript", "C#", "C++", "HTML", "CSS", "Python"];
var names = new Array("Mhmd", "Sami", "Nour", "Hiba");

var number = 12;

// languages.pop(); Remove last element
// languages.shift(); Remove first element
// languages.splice(3, 1); delete element by index and number of delete count
// languages.push("PHP"); add element to array
// languages.unshift("PHP"); add element to first of array
// languages.length = 8;
// languages.sort();
// languages.reverse();

// languages.splice(languages.indexOf("HTML"), 1);
// if (languages.includes("HTML")) {
//   console.log("Yes and index of it  " + languages.indexOf("HTML"));
// }

console.log(languages);
